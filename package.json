{
  "name": "neutrino-template",
  "version": "1.0.0",
  "description": "🌌 lightweight template for creating a website with module bundling hmr, typescript, tailwindcss & sass",
  "keywords": [],
  "scripts": {
    "dev": "mkdir -p dist && parcel src/**/*.html & ./scripts/create_manifest.sh && ./scripts/copy_icons.sh",
    "build": "mkdir -p dist && parcel build src/**/*.html --no-scope-hoist & ./scripts/create_manifest.sh && ./scripts/copy_icons.sh"
  },
  "dependencies": {
    "parcel": "^2.12.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.263",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
}
